 /**      @file: soma.c
 *     @author: Guilherme N. Ramos (gnramos@unb.br)
 * @disciplina: Algoritmos e Programação de Computadores
 *
 * Implemente a função soma. */

#include <stdio.h>

/* Escreva uma função recursiva que recebe como argumento um
 * inteiro positivo n e retorna a soma de todos os números
 * inteiros entre 0 e n. */
int soma(int n) {
    /* Implemente o código aqui. */
}

int main() {
    /* Implemente o código aqui. */

    return 0;
}