/**      @file: mdc.c
 *     @author: Guilherme N. Ramos (gnramos@unb.br)
 * @disciplina: Algoritmos e Programação de Computadores
 *
 * Implemente a função mdc_r. */

#include <stdio.h>

/* Retorna o Máximo Divisor Comum dos números dados,
 * computando de forma recursiva. */
int mdc_r(int a, int b) {
  /* Assume a >= b >= 0 */

  /* Implemente o código aqui. */
}

int main() {
    /* Implemente o código aqui. */

    return 0;
