/**      @file: ordena.c
 *     @author: Guilherme N. Ramos (gnramos@unb.br)
 * @disciplina: Algoritmos e Programação de Computadores
 *
 * Implemente a função ordena. */

#include <stdio.h>

/* Recebe três números e organiza seus conteúdos de modo que
 * haja uma ordem a <= b <= c. */
void ordena(int* a, int* b, int* c) {
	/* Implemente o código aqui. */
}

int main() {
    int a, b, c;

    /* Leia a, b, e c. */
    /* Execute a função "ordena" nos valores. */
    printf("%d <= %d <= %d\n", a, b, c);

    return 0;
}