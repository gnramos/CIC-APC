/**      @file: vetor/7-cartas_na_mao.c
 *     @author: Guilherme N. Ramos (gnramos@unb.br)
 * @disciplina: Algoritmos e Programação de Computadores
 *
 * Em um jogo de cartas há três times (5 jogadores em cada),
 * em que cada jogador dispõe de 3 cartas em cada mão. Defina
 * o vetor jogo que armazena todas estas informações e
 * implemente um algoritmo que mostre as cartas de cada
 * jogador. */

#include <stdio.h>

int main() {
    const int TIMES = /* ? */;
    const int JOGADORES = /* ? */;
    const int MAOS = /* ? */;
    const int CARTAS = /* ? */;
    char jogo/* ? */;

    return 0;
}