/**      @file: alocacao/2-N_strings.c
 *     @author: Guilherme N. Ramos (gnramos@unb.br)
 * @disciplina: Algoritmos e Programação de Computadores
 *
 * Implemente um programa que leia um número inteiro positivo N qualquer,
 * seguido de N strings (conforme o tipo definido), aloque dinamicamente a
 * memória para armazená-los, e então imprima esses N strings, indicando o
 * endereço inicial de cada string e seu comprimento. Assuma que há memória
 * suficiente para os N strings e que a entrada de dados que indica o fim do
 * string é a quebra de linha (aceite espaços entre os caracteres).
 *
 * Por exemplo, ao receber:
 *
 * 2
 * Hello, World!
 * Yes, nos temos bananas.
 *
 * o seu programa deve imprimir algo como:
 *
 * 1) Endereço: 0x6bd010 (13 caracteres)
 * 2) Endereço: 0x6bd080 (23 caracteres)
 *
 * (os valores dos endereços podem mudar) */

#include <stdio.h>

typedef char string[100];

int main() {

    return 0;
}