/**      @file: vetor/1-medias.c
 *     @author: Guilherme N. Ramos (gnramos@unb.br)
 * @disciplina: Algoritmos e Programação de Computadores
 *
 * Implemente as funções le_vetor, media, e mostra_acima, e
 * complete a função main para que o código funcione. */

#include <stdio.h>

/* Recebe um vetor e um inteiro positivo n, preenche o vetor
 * com n valores de notas lidas da entrada padrão. */
void le_vetor(/* ? */) {

}

/* Recebe um vetor e um inteiro positivo n, retorna a media
 * aritmética de seus elementos. */
float media(/* ? */) {

}

/* Recebe um vetor, um inteiro positivo n, e um valor de
 * média, e mostra quais os elementos que estão acima da
 * média. */
void mostra_acima( /* ? */) {

}

int main() {
    /* Defina notas (float) */

    printf("Digite as notas:\n");
    /* le_vetor notas */
    /* media notas */
    printf("A média das notas é: %f\n", media);
    printf("As notas acima da média são:\n",);
    /* mostra_acima */

    return 0;
}