/**      @file: registro/1-imagem.c
 *     @author: Guilherme N. Ramos (gnramos@unb.br)
 * @disciplina: Algoritmos e Programação de Computadores
 *
 * Supondo que uma imagem seja apenas uma matriz de pixels,
 * defina o registro file_info para conter as informações
 * sobre a imagem:
 *
 * resolucao: inteiro que define o tamanho (pixels) da imagem
 *   largura: quantidade de pixels horizontais da imagem
 *    altura: quantidade de pixels da imagem
 *   tamanho: inteiro que define o tamanho (bytes) da imagem
 *
 * Suponha que cada pixel ocupe 3 bytes de memória. */

#include <stdio.h>

int main() {

    return 0;
}

/* Não é mera semelhança...
https://msdn.microsoft.com/en-us/library/windows/desktop/dd183376%28v=vs.85%29.aspx */